"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.mapFlags = (obj, map = {}) => {
    const mapped = {};
    Object.keys(obj).forEach(k => {
        let key = map[k] || k;
        if (obj[k] || obj[k] === false) {
            const value = String(obj[k]);
            key = key.replace(/([A-Z])/g, (g) => `-${g[0].toLowerCase()}`);
            mapped[`--${key}`] = value;
        }
    });
    return Object.entries(mapped).reduce((arr, [k, v]) => {
        let val = k;
        if (v || v === false)
            val = `${val}=${v}`;
        arr.push(val);
        return arr;
    }, []);
};
//# sourceMappingURL=mapFlags.js.map