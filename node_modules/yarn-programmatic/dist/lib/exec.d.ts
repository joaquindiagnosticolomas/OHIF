/// <reference types="node" />
import { Options } from 'child-process-promise';
import { ExecOptions as CPExecOptions } from 'child_process';
export declare type ExecOptions = Options & CPExecOptions;
export declare const exec: (cmd: string, args: string[], options?: (Options & CPExecOptions) | undefined) => Promise<import("child-process-promise").PromiseResult<string>>;
