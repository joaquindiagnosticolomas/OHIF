import { ExecOptions } from '../lib/exec';
export interface AddOptions extends ExecOptions {
    dev?: boolean;
}
export declare const add: (packages: string | string[], options?: AddOptions) => Promise<void>;
