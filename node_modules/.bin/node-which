#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../../extensions/cornerstone-dicom-seg/node_modules/cross-env/node_modules/which/bin/node-which" "$@"
  ret=$?
else 
  node  "$basedir/../../extensions/cornerstone-dicom-seg/node_modules/cross-env/node_modules/which/bin/node-which" "$@"
  ret=$?
fi
exit $ret
